#Application Configuration
server:
  port: 8092
  error:
    include-message: always
    include-binding-errors: always
spring:
  data:
    mongodb:
      user: jsrz_pragma
      password: jsmithr
      uri: mongodb://practicecustomer.kudgh.mongodb.net/photo-customer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/